import { sortAnswers } from 'forum/helpers/sort-answers';
import { module, test } from 'qunit';

module('Unit | Helper | sort answers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sortAnswers([42]);
  assert.ok(result);
});
