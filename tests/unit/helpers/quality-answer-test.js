import { qualityAnswer } from 'forum/helpers/quality-answer';
import { module, test } from 'qunit';

module('Unit | Helper | quality answer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = qualityAnswer([42]);
  assert.ok(result);
});
